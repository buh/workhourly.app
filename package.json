{
  "name": "workhourly-website",
  "version": "1.0.0",
  "description": "WorkHourly website with multi-language support",
  "main": "template/build.js",
  "scripts": {
    "build": "node template/build.js",
    "build-dev": "node template/build.js --dev",
    "validate": "node template/validate.js",
    "build:validate": "npm run build && npm run validate"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}